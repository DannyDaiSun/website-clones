# Textures

Paper/parchment textures for the hand-drawn card aesthetic.
Currently using CSS gradients instead of image textures for performance.
Add optional `.png` or `.webp` textures here if needed in future phases.
